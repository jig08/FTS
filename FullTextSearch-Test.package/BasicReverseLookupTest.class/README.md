searchFacade builds the index

when you search for any of the tokens in the list, you want that the result includes the method selector

so that tells you , that for a given method selector there are multple tokens that "belong" to that selector."